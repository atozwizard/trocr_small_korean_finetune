# trocr_finetune

이 checkout은 `trocr_finetune` 프로젝트 전용 작업 공간이다.

원격 저장소는 `twentyflags.git` 하나를 공유하지만, 이 프로젝트의 history는 orphan branch인 `projects/trocr_finetune/main`에 분리되어 있다.

작업 전 확인:

```bash
git branch --show-current
git status --short
```

현재 브랜치는 반드시 다음이어야 한다.

```text
projects/trocr_finetune/main
```

